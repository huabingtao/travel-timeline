import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'export',  // 注释掉，dev server 需要
  // trailingSlash: true,
  // images: {
  //   unoptimized: true,
  // },
  // basePath: '/travel-timeline',
  // assetPrefix: '/travel-timeline/',
};

export default nextConfig;
